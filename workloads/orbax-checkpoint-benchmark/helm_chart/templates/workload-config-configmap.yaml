# yamllint disable
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name }}-config"
data:
  workload-configuration: |-
{{ .Files.Get "orbax_checkpoint_bench.py" | nindent 4 }}
  requirements.txt: |-
{{ .Files.Get "requirements.txt" | nindent 4 }}
